<form method="post" action="{{ path('admin_delete', {'id': post.id}) }}" onsubmit="return confirm('Vous êtes sur de vouloir effacer ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ post.id) }}">
    <button class="btn">Delete</button>
</form>
